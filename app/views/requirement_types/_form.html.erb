<%= form_with(model: requirement_type, local: true) do |form| %>
  <% if requirement_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(requirement_type.errors.count, "error") %> prohibited this requirement_type from being saved:</h2>

      <ul>
        <% requirement_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
