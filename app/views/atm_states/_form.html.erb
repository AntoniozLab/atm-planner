<%= form_with(model: atm_state, local: true) do |form| %>
  <% if atm_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(atm_state.errors.count, "error") %> prohibited this atm_state from being saved:</h2>

      <ul>
        <% atm_state.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :ATM_id %>
    <%= form.text_field :ATM_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
