<p id="notice"><%= notice %></p>

<h1 class="h4 mb-4 text-gray-800 font-weight-bold">Requerimientos</h1>
<div class="row">
  <div class="col-lg-10">
    <div>
      <p class="mb-4">Los requerimientos son las peticiones que realizan los usuarios para poder realizar una operación
        de
        instalación, retiro o movimiento de cajero.</p>
    </div>
    <div class="card shadow mb-4">
      <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Lista completa</h6>
      </div>
      <div class="card-body">
        <div class="table-responsive small">
          <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
            <thead>
              <tr>
                <th>Descripción</th>
                <th>Fecha</th>
                <th>Usuario</th>
                <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tfoot>
              <tr>
                <th>Descripción</th>
                <th>Fecha</th>
                <th>Usuario</th>
                <th></th>
                <th></th>
                <th></th>
              </tr>
            </tfoot>
            <tbody>
              <% @requirements.each do |requirement| %>
              <tr>
                <td><%= requirement.title %></td>
                <td><%= requirement.request_date %></td>
                <td><%= User.find(requirement.user_id).name %> <%= User.find(requirement.user_id).last_name %></td>
                <td><%= link_to 'Show', requirement %></td>
                <td><%= link_to 'Edit', edit_requirement_path(requirement) %></td>
                <td>
                  <%= link_to 'Destroy', requirement, method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
              <% end %>

            </tbody>
          </table>
        </div>
      </div>
    </div>
    <!-- Links inferiores -->
    <hr />
    <ul class="nav small">
      <li class="nav-item">
        <%= link_to 'Nuevo requerimiento', new_requirement_path, class: "btn btn-primary btn-sm" %>
        <!-- <a href="<%= requirements_path %>" class="nav-link py-1 px-2">Crear requerimiento</a> -->
      </li>
    </ul>
  </div>
</div>