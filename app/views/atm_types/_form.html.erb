<%= form_with(model: atm_type, local: true) do |form| %>
  <% if atm_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(atm_type.errors.count, "error") %> prohibited this atm_type from being saved:</h2>

      <ul>
        <% atm_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :ATM_id %>
    <%= form.text_field :ATM_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
