<div class="items">
  <div class="block-form">
    <fieldset-standard>

      <%= form_with(model: atm_brand, local: true, html: {class: 'admin' }) do |form| %>
      <% if atm_brand.errors.any? %>
      <div id="error_explanation">
        <h3 class="text-danger"><%= pluralize(atm_brand.errors.count, "error(es)") %> al registrar.</h3>
        <ul>
          <% atm_brand.errors.full_messages.each do |message| %>
          <li><%= message %></li>
          <% end %>
        </ul>
      </div>
      <% end %>

      <div class="field">
        <div class="items">
          <div class="block-100">
            <%= form.label :Marca %>
          </div>
          <div class="block-100">
            <%= form.text_field :name, { :class => "form-control"} %>
          </div>
        </div>
      </div>

      <div class="field">
        <div class="items">
          <div class="block-100">
            <%= form.label :Descripción %>
          </div>
          <div class="block-100">
            <%= form.text_field :description, { :class => "form-control"} %>
          </div>
        </div>
      </div>

      <div class="items">
        <div class="block-50-center">
          <%= form.submit "Guardar", :class => "btn btn-success" %>
        </div>
        <div class="block-50-center">
          <%= link_to 'Atrás', atm_brands_path, :class => "btn btn-info" %>
        </div>
      </div>
      <%end%>

      </fieldset>
  </div>
</div>